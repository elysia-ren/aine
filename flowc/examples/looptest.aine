fn main() {
    var i = 0
    while i < 3 {
        i += 1
    }
    print(f"i={i}")
    var s = "abc"
    var j = 0
    while j < s.len() {
        print(f"ch={s[j]}")
        j += 1
    }
    print("looptest done")
}

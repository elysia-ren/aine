# Aine Language Reference

> **版本**：V0.1（初版）
>
> **状态**：本版为《Aine 语言完整设计方案定稿版》（V5.8）之后的第一版正式语言参考，**收录 D1-D13 定稿决策的迁移内容**（见《Aine 设计决策登记簿》定稿版）。
>
> 本版不完整：词法、完整 Grammar、类型系统细则、求值顺序等章节将在 Phase 0-2 实现过程中填充（对应交付物 12 的完成定义）。
>
> 已收录章节（本节中每条标注来源决策编号）：
>
> - §1 值语义与优化承诺（D2）
> - §5 字符串类型（D7）
> - §6 引用与借用（D5）
> - §8 错误处理（D9）
> - §9 资源释放（D13）

---

## §1 值语义与优化承诺

> **来源：登记簿 D2（🟢 已确认）**

### 1.1 正式承诺措辞

> **零额外分配是优化目标，不是语言保证；语言保证的是——不产生『不必要』的完整数据复制，且任何物化都有可解释的诊断。**

- 拥有值语义（`String`、`Vec<T>` 等）是稳定的用户语义（V5.8 §6）；
- 借用 / Compiler View 是编译器可选择的实现优化（V5.8 §7-§8），优化失败不是语义错误（V5.8 §8.3）；
- 精度降级不改变程序可观察语义（见 Compiler Internals §1 架构约束）。

### 1.2 View-eligible 返回规则

编译器仅在满足**全部**条件时允许对返回值采用 Borrow/View，否则 Materialize：

1. 所有 return 分支的值可追踪到同一调用作用域内的参数/局部字段投影；
2. 返回值在调用点未被存储、未跨任务 / UI 边界；
3. 无动态索引 / 不确定 provenance（§71.3 场景默认不 eligible）。

### 1.3 物化诊断阈值

| 档位 | 单次物化对象大小 | 行为 |
|---|---|---|
| Cheap | < 64 KB | 静默，不产生提示 |
| Material | 64 KB - 1 MB | 非阻塞性能提示（默认开，可配置关闭） |
| Large | > 1 MB | 强制提示 + IDE 物化仪表盘计数 |

> 阈值按"单次物化对象大小"计，可全局配置（aine 配置）。

---

## §5 字符串类型

> **来源：登记簿 D7（🟢 已确认）**

### 5.1 类型清单

| 类型 | 定位 | 用户可见性 |
|---|---|---|
| `String` | 默认拥有字符串类型（V5.8 §6.1） | ✅ 用户可见 |
| `&str` | 高级借用视图 | ✅ 用户可见（高级 API） |
| `str_view` | 编译器内部表示 | ❌ 用户不可见 |

### 5.2 `str_view` 规则

1. `str_view` 是**编译器内部表示类型**：不出现在用户可见 API、不出现在用户源码、不出现在文档与 IDE 显示（IDE 一律显示 `&str` 或 `String`）；
2. 标准库高性能 API（如 `read_view` 类）的公开返回类型为 `&str`（借用内部缓冲区）或拥有值；内部实现可使用 `str_view`；
3. 用户在源码中书写 `str_view` → **编译错误**，提示"请使用 `String` 或 `&str`"；
4. 诊断文本禁止出现 `str_view` 字样（由诊断模板层过滤）。

### 5.3 字符索引模型（D15 定稿）

字符串按**字符**（Unicode 码点）计量：`s.len()` 为字符数，`s[i]` 为第 i 个字符，
切片 `s[a..b]` 按字符位；UTF-8 仅为内部表示，不进用户心智。索引为 O(i)
（确定性成本）。中文示例：`"你好Flow".len() == 6`、`"你好Flow"[0] == "你"`。

---

## §6 引用与借用

> **来源：登记簿 D5（🟢 已确认）**

### 6.1 引用类型的使用边界

- `&T` / `&mut T` 是高级精确 API（V5.8 §22.3），仅可作为**参数与局部变量**；
- **两档模型（D5 修订）**：普通模式所有权自动推导，无需书写返回借用；高级精确借用层允许 `&T` 出现在高级函数签名与返回位置（如 `fn get_name(user: &User) -> &str`），但必须通过完整的逃逸与借用检查；生命周期参数仍不可见（Language Veil 不破例）。普通代码编译器不要求（也默认不生成）借用返回。

### 6.2 按值参数 → `&T` 的隐式 reborrow

- **允许**：按值参数在调用点隐式传给 `&T` 参数（编译器创建仅限于本次调用的临时视图）：

```flow
fn inspect(user: &User) { ... }

fn main() {
    let user = User { ... }
    inspect(user)   // 合法：隐式临时视图，生命周期 = 本次调用
}
```

- **禁止**：按值参数隐式传给 `&mut T` 参数——要求调用方先 `var` 绑定或显式写 `&mut`。
- **`&T` 参数不得被存储**到超出本次调用作用域的容器（`Vec<&T>` 等）：编译器检查并报错，提示改用拥有值。临时视图生命周期恒等于本次调用。

### 6.3 `move` 与显式所有权转移

`move` 表示明确要求所有权转移（V5.8 §22.2），不受本节 reborrow 规则影响。

---

## §8 错误处理

> **来源：登记簿 D9（🟢 已确认）**

### 8.1 错误模型总览

| 机制 | 用途 |
|---|---|
| `Option<T>` | 有 / 无（V5.8 §45） |
| `Result<T, E>` | 成功 / 失败（V5.8 §45） |
| `?` | Try 传播（V5.8 §45），含 `Task<Result<T,E>>` 的统一 Try 语义（§46） |
| `try/catch` | 仅 FFI / 平台异常 / `#[catchable]` 函数 |

### 8.2 `try/catch` 捕获边界

1. **默认 panic 不可捕获**：普通代码 `catch` 一个非 FFI、非 `#[catchable]` 的 panic → **编译期拒绝**；
2. 仅 `extern` 函数（FFI）与显式标注 `#[catchable]` 的函数可被 `catch` 捕获：

```flow
try {
    native_call()?
} catch (e: FfiError) {
    handle(e)
}
```

3. `#[catchable]` 仅允许在 `unsafe` / FFI 边界模块内使用；普通模块不可标注；
4. 业务失败一律 `Result`（V5.8 §47）。

---

## §9 资源释放

> **来源：登记簿 D13（🟢 已确认）**

### 9.1 `defer` 语义

- `defer` 在所有退出路径（正常返回、early return、panic unwinding、可捕获异常）**按 LIFO 执行**；
- panic 被捕获后，已执行的 `defer` **不回滚**；
- **`defer` 内禁止再次 panic**：触发 abort 语义（文档明确，非可捕获路径）。

### 9.2 确定性析构

局部资源的析构顺序与 `defer` 栈一致（函数退出 → defer LIFO → 局部资源析构，V5.8 §27）。

---

## §10 模块系统（自举期语义，B5-M26 定稿）

> 完整命名空间语义按设计稿 §71.2 在 MVP 后演进；本节为自举期（MVP 前）定稿行为。

### 10.1 文件模块

```flow
module strutil;     // 读入同目录 strutil.aine（G2.0-③）
```

- 项以**原名**进入全局命名空间（扁平注册）；
- 递归加载，环路检测（重复加载报错）；
- 搜索路径：当前文件同目录 → `stdlib/`（标准库）。

### 10.2 import

```flow
import strutil      // 声明标记
```

- 名字已存在（如 `module x;`）时引用之，不存在时占位注册；
- 扁平语义下无命名空间效果。

### 10.3 内联模块

`mod x { ... }` 同为扁平注册：内部项以原名进入全局命名空间。

## §11 闭包

### 11.1 独立闭包值

```flow
let f = |x: i32| x * 2
print(f(21))                        // 42
```

- 参数**必须带类型注解**（独立闭包值）；
- 非捕获闭包：编译器直接脱糖为顶层函数（调用点零改动）；
- 捕获闭包：捕获变量提升为隐藏前缀参数，调用点自动补传（解释器与
  原生发射双侧已支持，A2 闭环）；捕获即快照（D14）——闭包值复制时
  快照独立，无共享可变状态。

### 11.2 方法链内联闭包

`nums.iter().map(|x| x * 2).sum()` —— 内联闭包参数类型可省略（由迭代元素推断）。

### 11.3 已知限制

f-string 内**直接**书写方法链（`f"{v.iter().map(...).sum()}"`）在原生编译路径
暂不支持（解释器完整支持）；等价写法：先 `let s = ...; f"{s}"`。

### 11.4 统一函数类型（D14 定稿）

```flow
fn apply(f: fn(i32, i32) -> i32, a: i32, b: i32) -> i32 {
    return f(a, b)
}
apply(add, 3, 4)          // 具名函数
apply(|x: i32, y: i32| x - y, 10, 4)   // 闭包同型
```

- **一个统一类型** `fn(A, B) -> R`：具名函数、非捕获闭包、捕获闭包同型，
  开发者永不区分（无 Fn/FnMut/FnOnce 三分法，无生命周期参数）；
- **捕获即快照**：闭包创建时对捕获变量取值快照；闭包值被复制时快照独立
  复制——无共享可变状态，`FnMut` 类问题在语言层面不存在；
- 原生路径：非捕获形态发射为 C 函数指针；捕获闭包的一等传递暂不支持
  （等价写法：显式参数传入）。

### 11.5 f-string 插值内嵌套引号

插值 `{...}` 内允许直接书写字符串（含与外层相同的引号）：

```flow
f"语言={m["name"]}"     // 直接可用，无需中转
```

## §12 测试（M23 定稿）

### 12.1 `@test` 属性与 `aine test`

```flow
@test
fn add_basics() -> Result<i32, String> {
    return expect(add(1, 2) == 3, "add(1,2) 应该是 3")
}
```

```text
aine test examples/testdemo.aine
```

- `@test` 标注**无参函数**即测试函数；按源码顺序发现（含 `mod` 平铺注册的模块内测试）；
- **失败判定**（三选一即失败）：
  1. 返回 `Result` 且为 `Err(...)` —— Err 载荷即失败原因；
  2. 运行时错误（如索引越界）—— 错误文本即失败原因；
  3. 崩溃 —— 其余测试已被隔离，不受影响；
- 正常返回（含 `Ok`、无返回值）即通过；
- 运行前先过完整前端流水线（词法/语法/解析/类型检查），前端错误不进测试。

### 12.2 输出与退出码

```text
运行 5 个测试 (examples/testdemo.aine):
  ✓ add_basics
  ✗ wrong_sum_fails
      原因: 返回 Err(1+1 竟然不等于 3)
测试结果: 4 通过, 1 失败 (共 5 个)
```

有失败时退出码非 0（脚本/CI 可判定）。

### 12.3 隔离模型

每个测试在**全新解释器实例 + 独立大栈线程**中运行：全局状态互不污染、
单个测试崩溃不影响后续测试、执行顺序确定（源码顺序）。

### 12.4 调试开关

运行时错误默认只报告 Aine 层信息（idx/len/内容/函数栈）。设置环境变量
`FLOWC_RT_BACKTRACE=1` 可在索引越界消息后附加宿主机栈帧（仅调试解释器
自身时需要，普通用户不可见）。

## §13 断言与索引赋值（M4 定稿）

### 13.1 `assert` 内建

```flow
assert(cond)              // 失败: 断言失败: 条件为假 （于函数 f）
assert(cond, "说明文字")   // 失败: 断言失败: 说明文字 （于函数 f）
```

- `assert` 失败即 panic（确定性、不可捕获），与索引越界同一资格模型；
- 第一个参数必须为 `bool`，否则编译期拒收（类型检查）；
- 测试框架中 assert 失败 = 该测试失败，原因逐字报告。

### 13.2 索引赋值（值语义下的原地写入）

```flow
var v = [3, 1, 2]
v[0] = 99                 // Vec 元素写入
v[1] += 10                // 复合赋值同支持
var m = Map.new()
m["key"] = 1              // Map 写入（键为 String）
```

- 资格模型与整体赋值一致：目标变量必须 `var`（否则 T3010）；
- **值语义保证**：`let b = a; a[0] = 9` 之后 `b` 不变——副本写入互不影响，
  无别名可能（Aine 无引用）；
- String 不开放局部写入（保持整体值语义）：`s[i] = x` 编译期拒绝，
  提示用切片拼接构造新串。

## 附录 A：本版迁移来源

| 章节 | 来源决策 | 状态 |
|---|---|---|
| §1 | D2 | 🟢 已确认 |
| §5 | D7 | 🟢 已确认 |
| §6 | D5 | 🟢 已确认 |
| §8 | D9 | 🟢 已确认 |
| §9 | D13 | 🟢 已确认 |

## 附录 B：待填充章节（Phase 0-2 期间）

词法、Grammar、类型系统、类型推导、求值顺序、Ownership 细则、闭包捕获、泛型、trait、模块、Task/Future、UI Domain、FFI 细则。

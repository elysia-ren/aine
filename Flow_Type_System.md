# Flow 类型系统规范（Type System Specification）

> **版本**：T1.0（M1.4 定稿，与 flowc 0.1.0 类型检查器同步）
>
> **地位**：Flow 类型系统的正式规范。对应设计稿 §22 函数参数、§45 错误处理、§55 基本语法；
> 交付物：21 Specifications（Type System Specification）。
> 语法根基见《Flow_Language_Grammar.md》§2.2（类型文法）。
>
> **总原则**（设计稿 §77 五条核心原则之第二、五条）：
> - 普通开发者面对值，不面对生命周期；类型检查器在无法确定时**安全回退为 Unknown**，
>   而不是把复杂度转嫁给开发者（渐进类型：明确的错误必报，未知库面不误报）。
> - AI 永远不是信任根：类型规则全部确定性。

---

## 1. 类型集合

### 1.1 基本类型

| 类型 | 说明 |
|---|---|
| `i32` / `i64` | 有符号整数（默认整数字面量为 i32） |
| `u8`-`u64` / `usize` | 无符号整数 |
| `f32` / `f64` | 浮点（默认浮点字面量为 f64） |
| `bool` | 布尔（true/false） |
| `char` | 字符 |
| `String` | **拥有**字符串（设计稿 §6.1；`"..."` 与 `f"..."` 均为 String） |

### 1.2 复合类型

| 类型 | 说明 |
|---|---|
| `Vec<T>` | 动态数组；`[]` 推断为 `Vec<Unknown>`；嵌套 `Vec<Vec<T>>` 支持 |
| `Map<K, V>` | **有序**键值对（插入序遍历）；键恒为 `String`；`Map.new()` 创建，
  `set/get/contains/keys/values/len` 方法；嵌套值类型 `Map<String, Vec<T>>`、
  `Map<String, Map<String, T>>` 支持（自举 B5-M25） |
| `Option<T>` | 有/无（设计稿 §45）；变体 `Some`/`None` |
| `Result<T, E>` | 成功/失败（§45）；变体 `Ok`/`Err` |
| `(A, B, ...)` | 元组；`()` 为单元类型 |
| `&T` / `&mut T` | 引用（高级 API，登记簿 D5：仅参数/局部，禁止返回与存储） |
| 命名类型 | 用户 `struct` / `enum` / `ui` 组件 |
| `Unknown` | 未确定类型（渐进回退；方法调用、内置函数、未知库面） |
| `Error` | 类型检查失败后的污染类型（抑制级联误报） |

---

## 2. 字面量默认类型

```text
123       : i32
123_456   : i32
0x1F     : i32
1.5      : f64
"..."    : String
f"..."   : String（求值阶段解析插值）
true     : bool
[]       : Vec<Unknown>
[1, 2]   : Vec<i32>
["a"]    : Vec<String>
(1, "a") : (i32, String)
```

---

## 3. 表达式类型规则

| 构造 | 规则 | 错误码 |
|---|---|---|
| `a + b`（加减乘除模） | 两侧同为数值（整/浮）；Unknown 回退 | T3002 |
| `a < b` 等比较 | 两侧同为数值 | T3002 |
| `a && b` / `a \|\| b` | 两侧同为 bool | T3002 |
| `a & b`、`<<`、`^` | 两侧同为整数 | T3002 |
| `-x` | x 为数值 | T3002 |
| `!x` | x 为 bool | T3002 |
| `if cond { }` / `while cond` | cond 为 bool（Unknown 允许） | T3003 |
| `let x: T = e` | e 可赋给 T（Unknown 允许） | T3001 |
| `x = e` / `x += e` | 目标可赋值；x 非 mut 绑定 → T3010；类型不匹配 → T3005 | T3010/T3005 |
| `f(args)` | 可解析的函数签名：参数个数 → T3008；返回类型 | T3008 |
| `x.method(...)` | 接收者类型化；方法结果 Unknown（stdlib 方法表后续） | — |
| `S { f: e }` | S 为已知 struct：字段存在 → T3006；字段类型 → T3007 | T3006/T3007 |
| `match e { }` | 各臂类型一致（Unknown 允许） | — |
| `e?` | Option(T)→T；Result(T,E)→T；Unknown→Unknown | — |
| `return e` | e 可赋给声明返回类型；无返回类型但有值 → T3004 | T3004 |

---

## 4. 赋值兼容（Assignable）

`A` 可赋给 `B` 当且仅当：

1. `A == Unknown` 或 `B == Unknown`（渐进回退）；
2. 或 `A == B`；
3. 或结构递归相等（Vec/Option/Result/元组逐元素）。

**不允许隐式数值转换**（i32→i64 需显式；`to_f64()` 等为 stdlib 方法）。

---

## 5. 函数与模块

- 函数签名类型：`(ParamT, ...) -> RetT`；参数缺注解时为 Unknown。
- 声明返回类型缺省：块尾值类型；无尾值时 `()`。
- `move` 参数：所有权转移语义（登记簿 D1-D13 范围外，M2 值流处理）。
- 模块/UI 组件在值位置作为其类型名使用（`run_ui(AccountBook)`）。

---

## 6. 错误处理与回退

| 错误码 | 级别 | 含义 |
|---|---|---|
| N3003 | 警告（M1.4 保持；转为错误需先补全 stdlib 类型表） | 未定义类型名 |
| T3001-T3010 | 错误 | 类型不匹配等，见 §3 表 |
| Unknown 回退 | — | 方法调用/内置函数/未知类型一律 Unknown，不误报 |

> N3003 保持警告的原因：设计稿 §69 示例使用未定义类型 `AppError`；
> 待 stdlib 类型表补全后（Phase 2）将强化为错误。

---

## 7. 与后续阶段的关系

- M2 值流分析：在类型化 AST 上运行，`Unknown` 视为不透明拥有值。
- M1.5 Formatter：不依赖类型，仅语法。
- Phase 2 泛型/trait：在本规范的命名类型基础上扩展（`T: Trait` 约束）。

# Aine 语法优化清单（自举实战驱动）

> 来源：用 Aine 写 lexer/json/parser 过程中暴露的全部语法/工具问题。
> 原则：语法保持简洁高效（不引入复杂类型系统）；每个问题标注处置结论。

## 一、已解决（48 个，均已回写 Grammar G1.0 或实现）

| # | 问题 | 性质 | 处置 |
|---|---|---|---|
| 1 | String 比较被拒（c >= "0"） | 实现 | 允许 Str-Str 字典序 |
| 2 | a || b 的 || 误判尾部闭包 | 实现 | 尾部闭包仅单 | |
| 3 | 嵌套块尾部误按函数返回检查 | 实现 | 仅函数体 tail 检查 |
| 4 | Vec 方法不可变（push 丢弃） | 设计 | mut 接收者重绑定 |
| 5 | ? 传播错误（嵌套 Err 冒顶） | 实现 | call_fn 捕获 Propagate |
| 6 | match 臂不支持语句 | 语法 | 语句臂包块 |
| 7 | Some/None/Ok/Err 模式不匹配 | 实现 | 模式匹配补充 |
| 8 | typeck 拒 String+String | 语法 | Str+Str → Str |
| 9 | match 发散臂污染类型 | 实现 | 发散臂跳过合并 |
| 10 | 单元变体值/模式消歧 | 语法 | JNull 特判 |
| 11 | None 裸值 | 语法 | eval Ident 支持 |
| 12 | 表达式块内 return 不传播 | 实现 | RtError::Return |
| 13 | 无元组解构 .0/.1 | 语法 | 用结构体（不改语法） |
| 14 | 无 is 关键字 | 语法 | 用 match（不改语法） |
| 15 | let mut 顺序解析 | 已移除（G2.0-①） | 可变绑定唯一形态 var |
| 16 | += 等复合赋值 | 实现 | tokenize 补两字符 |
| 17 | 空元组 () 无法解析 | 语法 | EUnit |
| 18 | 泛型逗号截断类型 | 实现 | parse_ty 深度感知 |
| 19 | if !x { 一元 trailing 消费 | 实现 | parse_expr_mode(13,false) |
| 20 | 结构体字面量缺失 | 语法 | EStruct |
| 21 | f-string tokenize | 实现 | 整体 token |
| 22 | for-in 集合迭代 | 语法 | SFor 迭代表达式 + ERange |
| 23 | 解释器栈溢出 | 实现 | Rc<FnDef> + 大栈线程 |
| 24 | 嵌套泛型 `>>` 被词法为移位符 | 实现 | 解析器闭合括号拆分（pending_gt） |
| 25 | 转译器 second_type_arg 丢嵌套 `<>` | 实现 | 深度计数重写（B5-M25） |
| 26 | 闭包参数类型注解 `|x: i32|` | 语法 | Rust/FLOW 两侧 parse_closure 支持 |
| 27 | parse_type 不认 `|` 终止符 | 实现 | parse_ty 增加 `|` 分隔 |
| 28 | 文件模块 `mod name;` + import | 语法 | 加载器 + 扁平注册（B5-M26） |
| 29 | 模块搜索路径（同目录→stdlib/） | 语法 | 双侧加载器统一 |
| 30 | 跨模块前向引用 | 实现 | resolve hoist/bodies 两阶段分离 |
| 31 | 跨文件 span 碰撞 | 实现 | 加载器改为文本级内联合并源 |
| 32 | C read_file 失败语义不一致 | 实现 | 统一 fail-visible 标记 |
| 33 | String.len 字段发射缺口 | 实现 | c_expr EField 对 String 发 strlen |
| 34 | Vec<String>/Vec<f64> 字面量发射 | 实现 | char*[] / double[] |
| 35 | int 型 .sum() 链类型误判 f64 | 实现 | expr_ctype 按闭包体类型特判 |
| 36 | 值语义缺失（var-from-var 共享） | 实现 | MOVE+置零 / CLONE 双路径（B5-M29） |
| 37 | match 尾构造调用丢返回值 | 实现+规避 | 语句式改写规避；转译器修复列 B5-M32 后续 |
| 38 | 闭包捕获（隐藏参数提升） | 语法+实现 | desugar 捕获分析 + 调用点改写 |
| 39 | 解释器错误缺定位信息 | 实现 | fn 栈跟踪 + 越界 idx/len/内容 + 调用者标签 |
| 40 | 文件读取偶发失败（AV 竞态） | 实现 | read_file 加重试 |
| 41 | 诊断无建议/Quick Fix | 实现 | typeck 5 类高频错误挂白话建议 |
| 42 | let/if 诊断片段跨行吞行 | 实现 | parse_let span 修正 + 渲染单行钳制 |
| 43 | 未定义名无 did-you-mean | 实现 | 编辑距离 ≤2 提示相近名 |
| 44 | 语义错误阻断后续 typeck | 实现 | check 一次输出全部问题 |
| 45 | stringify 丢 f-string 前缀 | 实现 | EStr 臂保留 f"..." 原文（幂等修复） |
| 46 | 无 flowpkg | 实现 | pkg_tool：toml 解析 + build（M6 最小版） |
| 47 | var_info 比较表达式类型丢失 | 实现 | EBin ==/!=/</>/<=/>= → bool |
| 48 | f-string bool 插值输出 1/0 | 实现 | fl_bool 包装（bool 返回类型函数） |

## 二、评估后**不需要改**的（保持简洁）

| # | 提议 | 结论 | 理由 |
|---|---|---|---|
| 1 | char 类型 + 'a' 字面量 | **不做** | 字符=单字符 String 已是简洁设计；s[i] 索引已支持 |
| 2 | 元组值/解构 | **已有** | (1, "a") 字面量与显示已支持 |
| 3 | let 类型推断 | **已有** | let x = 42、空集合均推断；Vec<_> 支持 |
| 4 | 单字符索引 s[i] | **已有** | 返回单字符 String |
| 5 | 用户泛型/trait | 后置 | Phase 2（M4）范畴 |
| 6 | 标准字符串方法 | 后置 | 标准库扩充，非语法 |
| 7 | defer/go/ui 执行 | 后置 | Phase 3（M5）范畴 |
| 8 | f-string 求值插值 | 后置 | 解释器已有 eval_fmt 骨架 |

## 三、结论

- 语法层面**无需大改**：G1.0 在实战中经受住考验，23 个问题中 15 个是实现缺口、
  8 个是语法增补（均已回写规范）。
- 真正的摩擦点集中在**标准库缺失**（字符串方法）与**Phase 2 能力**（泛型/trait），
  属于 M4/M5 里程碑范畴，符合执行计划导向。
- 自举的价值已充分体现：**写 Aine 组件 = 对规范做差分测试**，问题在真实代码中暴露、
  修复后回写 Grammar，语法随实战收敛而非凭空设计。

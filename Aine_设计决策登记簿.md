# Aine 设计决策登记簿（Design Decision Register）— 定稿版

> **配套文档 1 / 3**
>
> 本登记簿收录《Aine 语言完整设计方案定稿版》（V5.8）定稿之后、工程实现之前**必须补齐的定义级决策**。
>
> V5.8 明确"原则上不再增加新的核心语言概念"，因此本登记簿**不新增语法概念**，只把设计稿中"未正面回答、但实现必须确定"的问题逐条定稿。
>
> 每条格式：**现状 → 问题 → 定稿决策（含规则草案）→ 影响 → 验收方式 → 评审定稿**。
>
> 评审记录：本版为 **D1-D13 逐条评审定稿版**（D12 原为"事件回调错误处理"，已并入 D3，故编号为 D1-D11 与 D13）。所有条目状态 **🟢 已确认**，结论已迁入正式文档（见各条【评审定稿】的迁移目标）。
>
> 迁入目标文档：
>
> - 《Flow_Language_Reference.md》（语言参考）
> - 《Flow_Compiler_Internals.md》（编译器内部）
> - 《Flow_Concurrency_Guide.md》（并发指南）
> - 《Flow_UI_Guide.md》（UI 指南）

---

## A 组：值语义与优化

### D1　标准库如何接收 Compiler View（"可读值"参数机制）

- **现状**：§7 允许 `print(user.name)` 内部走 Compiler View；§63 区分普通 API 与性能 API。但标准库函数签名形式未定义。
- **问题**：若 `fn print(s: String)`，编译器传入视图需要参数"能接受视图"；若改写成 `&str`，普通开发者被迫提前接触借用语法（违背 §3.1）。
- **定稿决策（🟢 已确认）**：
  1. **不引入任何用户可见的新类型**（否决"Readable<T>"方案，避免新语法）。
  2. 编译器规则：**按值参数若满足全部视图化条件，编译器在 lowering 时自动以视图实现该参数**；用户源码不可见。
  3. 视图化条件（**全部满足**才允许）：
     - 函数体对参数**只读**（无写入、无 move、无存储到状态/容器）；
     - 参数**未逃逸**（未进入任务/UI 操作/闭包捕获后被存储）；
     - 调用方实参可追踪为本地/字段投影（View-eligible，见 D2）；
     - **非泛型函数**（第一版泛型参数一律按值语义，不做视图化）。
  4. 标准库规范：只读 API（`print`、`Text`、`contains`、`starts_with` 等）签名保持按值形式，文档标注 `@readonly`（仅文档/IDE 用途，非语法）。
  5. 视图化失败的场景自然落入 §9 的 Materialize 回退。
  6. 显式需要引用时仍写 `&T`（高级 API，见 D5）。
- **影响**：编译器新增"只读参数视图化"lowering pass；标准库大部分字符串/容器只读 API 受益；用户语法零成本。
- **验收**：Conformance 用例——`print(user.name)` 对 100 MB String 的 Allocation Count == 0；`flow explain` 能解释视图化或物化原因。
- **评审定稿**：✅ 确认。细化：视图化条件中"非泛型"为第一版硬限制，泛型视图化列入 Phase 2 后评估；`@readonly` 确认仅为文档标注，不进入语法与诊断。
- **迁移目标**：Compiler Internals §2（视图化 pass 规范）；Language Reference §5（值语义）。

---

### D2　多来源返回的 View-eligible 规则与"零拷贝"承诺措辞

- **现状**：§24 多来源返回"能借用则优化，不能证明则 Materialize"；§72 验收措辞为"0 unnecessary full-data copy"。
- **问题**：多来源 + 长期持有的返回值会常态性物化，诊断噪音可能很高；承诺边界需要被正式措辞固定。
- **定稿决策（🟢 已确认）**：
  1. 正式措辞（写入 Language Reference 前言）：**"零额外分配是优化目标，不是语言保证；语言保证的是——不产生『不必要』的完整数据复制，且任何物化都有可解释的诊断。"**
  2. **View-eligible 返回规则**（满足全部条件才允许 Borrow/View，否则 Materialize）：
     - 所有 return 分支的值可追踪到同一调用作用域内的参数/局部字段投影；
     - 返回值在调用点未被存储、未跨任务 / UI 边界；
     - 无动态索引 / 不确定 provenance（§71.3 场景默认不 eligible）。
  3. **物化诊断阈值（定稿数值）**：
     - Cheap 档：< 64 KB → 静默，不产生提示；
     - Material 档：64 KB - 1 MB → 非阻塞性能提示（默认开，可配置关闭）；
     - Large 档：> 1 MB → 强制提示 + IDE 物化仪表盘计数。
     - 阈值按"单次物化对象大小"计，可全局配置。
  4. §72 验收保持"0 unnecessary"，并新增用例：多来源返回 + 长期存储 → 允许 1 次 Materialize，但必须有对应诊断。
- **影响**：实现简单；诊断噪音受阈值控制；承诺边界清晰。
- **验收**：§71.4 大对象用例 + 诊断计数断言（提示次数 ≤ 预期）。
- **评审定稿**：✅ 确认。细化：Material/Large 档的提示文案统一引用 §58 模板（F2104 风格）；阈值默认值写入 aine 配置文档。
- **迁移目标**：Language Reference §1（承诺措辞）；Compiler Internals §3（View-eligible 规则 + 阈值表）。

---

### D6　摘要精度降级不变量（架构级）

- **现状**：§18 定义五级精度（Level 0-5），§19/§59 预算回退。但"精度变化对程序行为的影响"未显式声明。
- **问题**：实现者可能把"精度不足"实现成行为差异，破坏 §8.3 "优化失败不是语义错误"。
- **定稿决策（🟢 已确认）**：正式声明编译器不变量并写入 Compiler Internals §1：
  > **摘要精度只影响生成代码的质量（分配数、复制次数、代码大小），绝不改变程序的可观察语义。任何精度组合（Level 0-5、跨模块缓存缺失、预算回退）下，同一源码必须产生行为一致的合法程序。**
- **影响**：约束所有分析 pass 的架构性要求；使精度选择可以放心交给预算机制。
- **验收**：Conformance 专项——同一源码分别以 Level 0 / 3 / 5 和"缓存全部失效"模式编译，行为断言完全一致，仅允许分配/性能差异。
- **评审定稿**：✅ 确认，无修正。列为 Compiler Internals 第一条架构约束。
- **迁移目标**：Compiler Internals §1（架构约束）。

---

### D7　`str_view` 的出现规则

- **现状**：§6.2 说 `str_view`"只有语义明确时才允许出现"——边界模糊。
- **问题**：普通开发者看到 `str_view` 会困惑；`str` 已退出默认使用，`str_view` 不应成为用户可见类型。
- **定稿决策（🟢 已确认）**：
  1. **`str_view` 是编译器内部表示类型，不出现在用户可见 API、不出现在用户源码、不出现在文档与 IDE 显示**（IDE 一律显示 `&str` 或 `String`）。
  2. 标准库高性能 API（如 `read_view` 类）的公开返回类型为 `&str`（借用内部缓冲区）或拥有值；内部实现可使用 `str_view`。
  3. 用户在源码中书写 `str_view` → 编译错误，提示"请使用 `String` 或 `&str`"。
- **影响**：语言表面更干净，符合"编译器隐藏内部复杂度"（§3.1）。
- **验收**：语法拒绝测试（负例）；`read_view` 零分配用例（正例）。
- **评审定稿**：✅ 确认。细化：诊断文本同样禁止出现 `str_view` 字样（由诊断模板层过滤）。
- **迁移目标**：Language Reference §5（字符串类型）；Compiler Internals §2（内部表示）。

---

## B 组：借用与 API 互操作

### D5　按值参数 → `&T` 的隐式 reborrow；函数禁止返回 `&T`

- **现状**：§22 定义按值参数、`move`、`&T`/`&mut T`，但按值参数传给 `&T` 参数的转换规则未定义；§23 用户语法无生命周期参数，`&T` 返回值无法标注来源。
- **问题**：普通代码与高级 API 的互操作边界不清；`&T` 返回在无生命周期语法下悬而未决。
- **定稿决策（🟢 已确认）**：
  1. **允许**按值参数在调用点隐式传给 `&T` 参数（编译器创建仅限于本次调用的临时视图）：`inspect(user)` 合法。
  2. **禁止**按值参数隐式传给 `&mut T` 参数：要求调用方先 `let mut` 绑定或显式写 `&mut`。
  3. **所有函数（含非公开）一律禁止返回 `&T`**（编译器拒绝，提示改用拥有值或拆分函数）；`&T` 仅可作为参数与局部变量。理由：无生命周期语法，`&T` 返回无法安全表达来源；编译器 View 已覆盖绝大多数零拷贝需求。此规则在 1.0 内不开放例外。
  4. **`&T` 参数不得被存储**到超出本次调用作用域的容器（`Vec<&T>` 等）；编译器检查并报错，提示改用拥有值。临时视图生命周期恒等于本次调用。
- **影响**：普通代码到高级 API 的桥接顺畅；`&T` 使用面收窄但安全边界清晰。
- **验收**：正例（`inspect(user)`）与负例（`&mut` 隐式、`fn f() -> &T`、`Vec<&T>` 存储）测试。
- **评审定稿**：✅ 确认。补强第 4 条（`&T` 不得存储），消除隐式 reborrow 的唯一悬空路径。
- **迁移目标**：Language Reference §6（引用与借用）。

---

## C 组：并发与任务

### D4　用户类型 Send / Sync 的参与方式

- **现状**：§26 编译器自动检查，普通代码不手写 `T: Send`。但用户自定义类型的 Send 性来源、显式声明语法未定义。
- **问题**：跨模块泛型推断需要确定规则；否则"这个值不能安全地发送到后台任务"的诊断无法稳定产生。
- **定稿决策（🟢 已确认）**：
  1. **Send / Sync 自动推断（字段级）**：对用户 `struct` / `enum`（含各变体字段）自动推导；容器（`Vec<T>` 等）按类型参数推断；闭包捕获按捕获内容推断。
  2. 显式声明逃生舱：`#[not_send]` 标记（仅影响跨任务/跨 Domain 使用检查，不影响普通使用）。
  3. 诊断输出业务语言（§26 已定）；IDE 高级视图可展开显示完整约束链。
  4. 第一版**不引入** `unsafe impl Send` 等价物；FFI 边界产生的类型统一视为 `not_send`，需要跨任务时经包装类型显式声明。
- **影响**：实现为简化 auto-trait 机制；用户面仅一个属性。
- **验收**：Task / UI 边界负例测试；跨模块泛型正例测试。
- **评审定稿**：✅ 确认，无修正。
- **迁移目标**：Concurrency Guide §3（Send/Sync 规则）；Compiler Internals §4（推断规则）。

---

## D 组：UI 与状态

### D3　`ui {}` 内部规则集（读 state / 只读操作 / 嵌套任务 / 回调 `?`）

- **现状**：§36-41 定义 `ui {}` = UI Domain Operation；State Target 与 Task-owned payload 分离；State 写入 = Consume/Store。但 `ui {}` 内部读取 state、调用函数、嵌套任务、事件回调的错误传播均未定义。
- **问题**：§69 示例中 `ui { total = records.iter()... }` 读取 State Storage，`? ` 出现在 Button 回调内——这些行为必须有确定语义。
- **定稿决策（🟢 已确认）**：
  1. **`ui {}` 内对 `@state` 的读取 = 直接读 State Storage**（同一 UI Domain 内同步读取，无消息开销）；读取在操作执行时求值。
  2. **允许**：读取捕获的 Owned/Send payload 与 State；调用**只读操作**——判定口径：不产生新的任务、不发起 IO/外部副作用、不写 State 之外的状态。**禁止**：在 `ui {}` 内发起新的 `go{}` / `go!{}`（编译错误："请在 ui{} 外部创建后台任务"）；捕获普通外部借用（与 §38 一致）。
  3. 事件回调签名：`(Event) -> ()` 或 `(Event) -> Result<(), UiError>`；`? ` 在回调内传播到回调返回值，由 UI runtime 捕获并显示错误提示（不崩溃、不 panic）。§69 中 `amount_input.to_f64()?` 即此语义。
  4. `ui {}` 内读取已 move 的变量 = 编译错误（与 §39 一致）。
- **影响**：编译器新增 `ui {}` 作用域检查（禁嵌套任务、禁外部借用捕获、只读操作判定）；事件回调错误语义确定。
- **验收**：§71.7 UI 用例 + 新增负例（嵌套 `go{}` 必须被拒绝、读取已 move 变量必须报错）。
- **评审定稿**：✅ 确认。修正措辞：原"允许调用纯函数"细化为"允许只读操作"（避免与"纯函数"术语歧义——ui{} 内允许读取 payload/State，这并非严格纯函数，但禁止副作用）。回调无返回值时默认 `()`。
- **迁移目标**：UI Guide §4（ui{} 语义与回调）。

---

### D8　`@global` 的机制定义

- **现状**：§44 只规定了禁止项（不得绕过 Send/Sync/任务边界/State Domain），未定义机制。
- **问题**：没有机制定义，`@global` 会成为绕过安全模型的漏洞或永不可用。
- **定稿决策（🟢 已确认）**：
  1. `@global` = **全局 State Slot**（组件无关的 State Storage）：只允许存 Send 数据；生命周期 = 进程；热重载时按类型匹配保留。
  2. **声明位置**：模块顶层（`@global let mut x: T`），不隶属组件。
  3. 写入规则与 `@state` 一致：只能经 UI Domain Operation（`ui {}`）或受控消息写入；**后台任务不得直接借用/写 `@global`**（同 §34 硬规则）。
  4. 读取：UI 内直接读 State Storage；后台任务需 `snapshot`（同 §35）。
- **影响**：与 `@state` 复用同一 runtime 机制，实现成本低；安全模型无漏洞。
- **验收**：负例测试（后台任务写 `@global` 被拒）。
- **评审定稿**：✅ 确认。补充第 2 条（声明位置），消除语法歧义。
- **迁移目标**：UI Guide §5（全局状态）。

---

## E 组：错误与资源

### D9　可捕获 panic 的标记机制

- **现状**：§47 `try/catch` 仅用于 FFI 异常、平台异常、"明确允许捕获的 panic"。
- **问题**：何种 panic "明确允许捕获"未定义，`try/catch` 边界不可执行。
- **定稿决策（🟢 已确认）**：
  1. **默认 panic 不可捕获**：普通代码 `catch` 一个非 FFI、非 `#[catchable]` 的 panic → 编译期拒绝。
  2. 仅 `extern` 函数（FFI）与显式标注 `#[catchable]` 的函数可被 `catch` 捕获；`catch (e: FfiError)` 绑定 FFI 错误对象。
  3. `#[catchable]` 仅允许在 `unsafe` / FFI 边界模块内使用；普通模块不可标注。
  4. 业务失败一律 `Result`（§47 已定）。
- **影响**：`try/catch` 使用面极窄，符合设计定位。
- **验收**：语法/语义测试（普通 panic 捕获取消、FFI 异常可捕获）。
- **评审定稿**：✅ 确认。补充第 2 条的 catch 绑定语法示例。
- **迁移目标**：Language Reference §8（错误处理）。

---

### D13　`defer` 与 panic / 异常交互

- **现状**：§27 列出 `defer` 适用于正常返回、early return、panic、可捕获异常。
- **问题**：panic 路径下 `defer` 的执行保证需明确（是否 unwind？顺序？）。
- **定稿决策（🟢 已确认）**：**`defer` 在所有退出路径（含 panic unwinding）按 LIFO 执行**（与 Rust Drop 语义一致）；panic 被捕获后，已执行的 `defer` 不回滚；**`defer` 内禁止再次 panic**（触发 abort 语义，文档明确）。
- **影响**：资源释放确定性；实现需支持 unwind 路径的 defer 栈。
- **验收**：异常路径测试（正常/early return/panic/捕获后）。
- **评审定稿**：✅ 确认，无修正。
- **迁移目标**：Language Reference §9（资源释放）。

---

## F 组：运行时与平台

### D10　Codegen 后端策略

- **现状**：设计稿与交付物清单均未指定 codegen 后端。
- **问题**：五平台 + Skia 的联动需要确定后端路线，否则 aine 多平台路径无法启动。
- **定稿决策（🟢 已确认）**：**采用 LLVM**（成熟、五平台 target 覆盖、与 Skia/工具链生态衔接）；Codegen 作为独立模块（便于未来替换）；自研后端列为远期（非 1.0）。
- **配套策略**：
  - LLVM 版本锁定：跟随 LLVM 长期支持节奏（18 个月升级窗口），aine 发布时锁定并分发匹配工具链（签名、离线安装）；
  - 1.0 前不承诺 ABI 稳定（同一版本内二进制兼容；跨版本需重编译）；
  - 移动端经 LLVM 对应 target 输出（UI 后端另见 D11）。
- **影响**：aine 依赖 LLVM；打包与分发成本增加；多平台风险显著降低。
- **验收**：Phase 6 五平台编译冒烟测试。
- **评审定稿**：✅ 确认。补充版本锁定与 ABI 承诺两条配套策略。
- **迁移目标**：Compiler Internals §5（Codegen 策略）。

---

### D11　移动端 UI 后端

- **现状**：§51 仅确定桌面默认渲染后端为 Skia；移动端未定。
- **问题**：Android/iOS 的 UI 线程模型、渲染栈与桌面差异大，若与桌面同期承诺会拖垮 MVP。
- **定稿决策（🟢 已确认）**：**1.0 桌面（Skia）优先；移动端 UI 后端列为 1.1**；移动端第一阶段支持"纯逻辑应用 + 最小 UI 桥"（窗口/基本控件经平台原生桥），完整声明式 UI 后置。UI Domain 在移动端绑定主线程（平台惯例）。
- **影响**：MVP 聚焦桌面，风险可控；跨平台时间线后移但方向不变。
- **验收**：Phase 6 移动端里程碑（1.1）。
- **评审定稿**：✅ 确认，无修正。
- **迁移目标**：UI Guide §6（平台支持）。

---

## G 组：语言面补全（M4 · 2026-08 定稿）

### D14　统一函数类型与"捕获即快照"

- **背景**：高阶函数需要 fn 类型形参（`fn apply(f: fn(i32,i32) -> i32, ...)`）。
  若不定捕获语义，开发者将被迫区分"能传/不能传的函数"——Rust `Fn/FnMut/FnOnce`
  三分法与生命周期正是 Aine 承诺吸收的复杂度。
- **定稿决策（🟢 已确认）**：
  1. **一个统一函数类型** `fn(A, B) -> R`——具名函数、非捕获闭包、捕获闭包同型，
     开发者永不区分；类型系统不引入任何 trait 三分法；
  2. **捕获即快照（capture-by-value）**：闭包创建时对捕获变量取值快照；
     闭包值被复制时快照随之独立复制。无共享可变状态 → `FnMut` 类问题不存在；
  3. 闭包内修改捕获快照只影响本实例（需 mut 绑定），语义确定、可解释；
  4. 实现落点：解释器为创建时 env.snapshot()（既有行为，本决策将其正式化）；
     C 后端非捕获形态发射为普通函数指针，捕获闭包的一等传递为已知限制
     （隐藏参数提升机制下源层等价写法：显式参数传入）。
- **影响**：typeck `Ty::Fn`（签名宽松匹配）、双解析器 `fn(...)->...` 类型语法、
  C 参数声明 `R (*name)(A, B)` 形态；测试框架参数化由此解锁。
- **验收**：conformance/conf_language.aine c6_fn_type_*；case30（函数指针发射）。

### D15　字符串字符索引模型

- **背景**：字符串 `len`/`s[i]`/`s[a..b]` 的计量单位必须唯一确定
  （字节 vs 码点），混用是编码错误之源。
- **定稿决策（🟢 已确认）**：**按字符（Unicode 码点）**——`len()` 为字符数、
  `s[i]` 为第 i 个字符、切片按字符位；UTF-8 仅为内部表示，不进用户心智。
  索引为 O(i)（确定性成本，写入文档）。
- **影响**：解释器本已按字符实现（本决策正式化）；C 后端新增
  `fl_chars_len / fl_char_at / fl_substr_ch`（UTF-8 感知，已入 C 运行时备妥）；
  stdlib strutil 注释更新；ASCII 存量代码零破坏（单字节字符行为不变）。
- **已知分歧（后续项，本轮实测定位）**：C 后端的 len/索引/切片发射暂维持
  **字节形态**——自举代码内部依赖 len 与索引的自洽性（词法器等），
  若仅 len 改字符级会与字节索引混用，使含中文注释的源码在编译路径错位
  （实测：自宿主 case13 解析失败，token 减半）。C 侧统一切换到字符语义
  需要表达式级类型表（即 M6-TCK 的 Index-span 前置），列为本决策后续项；
  届时仅改发射即可（辅助函数已备）。解释器侧 len/索引/切片已是字符语义
  （多字节正确），为参考实现。
- **验收**：conformance c6_unicode_chars（解释器路径）；
  examples/zhstringtest.aine；自宿主 29 用例维持全过。

---

## 决策依赖关系

```text
D1（视图参数机制）
  └─ 依赖 D5（&T 互操作规则）
D2（View-eligible 返回 + 承诺措辞）
  └─ 依赖 D6（精度不变量，作为其语义基础）
D3（ui{} 规则集）
  └─ 依赖 D4（Send/Sync 推断，payload 检查）
D8（@global）复用 D3 的写入规则
D9 / D13（错误与 defer）独立
D10 / D11（codegen / 移动端）影响 Phase 6，不阻塞 Phase 0-3
```

## 定稿声明

> 本登记簿 D1-D11、D13 已于本次评审全部定稿（🟢 已确认），结论已迁入：
> Language Reference §1/§5/§6/§8/§9、Compiler Internals §1-§5、Concurrency Guide §3、UI Guide §4/§5/§6。
> 后续修改任何条目需经设计变更评审（与 V5.8 冻结原则一致）。

# Flow Concurrency Guide

> **版本**：V0.1（初版）
>
> **状态**：本版收录《Flow 设计决策登记簿》定稿版中属于并发的决策（D4），作为并发指南的第一版正式内容。
>
> 教学原则：**先教怎么用，再解释内部安全机制**（交付物 15）。
>
> 已收录章节：
>
> - §3 Send / Sync 规则（D4）
>
> 其余章节（`go{}` / `go!{}` / Future / Task / Channel / Cancellation / Structured Concurrency / UI Task 交互）将在 Phase 3 实现后填充。

---

## §1 概览（占位）

- `go {}`：创建并立即提交结构化 `Task<T>`（V5.8 §29）
- `go! {}`：detached background task（V5.8 §30）
- `Future<T>`：异步计算抽象（V5.8 §28.1）
- `Task<T>`：已提交调度的异步任务（V5.8 §28.2）
- Task 是所有权安全边界（V5.8 §31）：普通借用不得跨越

---

## §3 Send / Sync 规则

> **来源：登记簿 D4（🟢 已确认）**

### 3.1 用户视角（先教怎么用）

普通代码**不手写** `T: Send`（V5.8 §26）。跨任务 / 跨 UI Domain 传递值时，编译器自动检查；失败时输出业务语言诊断：

```text
错误：这个值不能安全地发送到后台任务。
原因：它包含了无法跨任务安全移动的数据（如平台句柄）。
建议：提取真正需要的数据（id / 查询参数）后发送；或使用 #[not_send] 显式声明（不推荐用于跨任务）。
```

### 3.2 自动推断规则

1. **字段级推断**：用户 `struct` / `enum`（含各变体字段）的 Send / Sync 性由字段组合自动推导；
2. **容器**：`Vec<T>` 等按类型参数推断；
3. **闭包捕获**：按捕获内容推断。

### 3.3 显式声明

- `#[not_send]`：显式声明某类型不跨任务（仅影响跨任务/跨 Domain 使用检查，不影响普通使用）；
- 第一版**不提供** `unsafe impl Send` 等价物；
- FFI 边界产生的类型统一视为 not_send；需要跨任务时经包装类型显式声明。

### 3.4 高级视图

IDE 可展开显示完整约束链（该类型因哪些字段、哪些类型参数而不 Send），供高级开发者排查。

### 3.5 边界要求（V5.8 §31/§40 摘要）

跨任务数据必须满足：`Owned` 或 `安全 Copy` 或 `满足 Send`；Task → UI Domain 的数据必须满足 `Owned + Send` 或 `Copy`；禁止传递任务局部借用、非 Send 对象、非法可变引用、裸指针。

---

## 附录 A：本版迁移来源

| 章节 | 来源决策 | 状态 |
|---|---|---|
| §3 | D4 | 🟢 已确认 |

## 附录 B：待填充（Phase 3 期间）

`go{}` / `go!{}` 教程、结构化并发、取消与副作用（V5.8 §32）、Channel、Async I/O、UI Task 交互完整指南。
